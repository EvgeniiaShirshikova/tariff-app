### Вопросы 💎

1. Какую ошибку я допустила в [этом](https://www.notion.so/23-CSS-React-d4b0b61bb697459aacb1681ba2764440?pvs=21) примере?

Return нет в const button?

2. Какие есть способы работы со стилями в React?

Инлайновый, в отдельных css файлах, CSS модули, CSS-препроцессоры

3. Как будет выглядеть карточка товара, если ей передать атрибут `addedToCart`===0?

Это значит, что в корзину она не добавлена, значит, будет кнока "добавить в корзину".

4. Какие ещё проверки нужно было бы сделать для атрибута `addedToCart`?

не знаю

5. Клиент попросил повесить тег «New» на товары из новой коллекции. Как это сделать, какой условный оператор выбрать?

isNew && "добавить тэг"

6. Какими тремя способами можно написать условный рендеринг? 

'if' ? 'then' : 'else'

'if' && 'then'

if {...} else {...}

7. Представьте, что вы пишете компонент логина. Если пользователь авторизован, то мы показываем его имя, а если нет, то даём возможность ввода логина и пароля. Какой код для этого нужно написать, если за авторизацию пользователя отвечает флаг isAutorized?

```jsx
    function AutorizeForm(props) {
    return (
        <form>
            <input className="login" value={props.login}/>
            <input className="password" value={props.password}/>
            <button className="button">Autorize</button>
        </form>
    );
}


export default function Card(props) {
	const {login, password, userName, imgLink, isAutorized} = props;
	return (
			  <div>
                {isAutorized ? props.userName : <AutorizeForm />}
            </div>
    );
}
```
    
- **примеси.** Набор правил, который мо


8. В чём преимущества использования препроцессоров? Какой ещё есть способ использовать переменные, кроме $ в препроцессорах?

Можно организовать блочную систему стилей, вынести общие стили в виде переменных и импортировать их в нужный файл; можно выполнять математические операции; вкладывать селекторы; амперсанд позволяет не повторять родительский селектор, быстро менять его. Помогает сделать код читабельней.

Про переменные не знаб других способов.